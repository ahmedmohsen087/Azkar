import 'package:flutter/material.dart';

class Royaa extends StatelessWidget {
  static const String routeName = 'Royaa';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('الرقية الشرعية '),
      ),

      body: ListView(
        children: [

        ],
      ),
    );
  }
}
